class Reader :
    def __init__(self, reader_id: int, name: str, age: int, gender: str):
        self.id = int(reader_id)
        self.name = str(name)
        self.age = int(age)
        self.gender = str(gender)

